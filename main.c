#include <stdio.h>
int main() {
   int nombre;
   char[100] nombre;
   printf("Hello, World!");
   printf("Nombre:")
   fgets(nombre, sizeof(nombre), stdin);
   printf("Edad:")
   scanf("d", edad);
   printf("%s tiene %d ", nombre, edad);
   return 0;
}